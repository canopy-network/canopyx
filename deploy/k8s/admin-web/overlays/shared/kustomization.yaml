apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Override for shared dev server mode:
# - Set NODE_ENV=production for optimized build
# - Strict health checks for production-like behavior
patches:
  - path: deployment-patch.yaml
