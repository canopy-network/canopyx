apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - ../../base

# Additional customizations for local development
labels:
  - pairs:
      environment: local
